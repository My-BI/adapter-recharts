# Acknowledgements

This adapter renders with **Recharts**, and would not exist without it.

- **Recharts** — https://recharts.org · https://github.com/recharts/recharts ·
  © the Recharts authors, licensed under the **MIT License**. The library is read from globals
  the host injects; it is not redistributed here.
- **Logo** — `logo.png` is the official Recharts project avatar
  (github.com/recharts), used to identify the upstream library. All trademarks belong to the
  Recharts authors; used for identification, not endorsement.

The MyBI **adapter** code (the per-library draw built against the MyBI chart host SDK) is
licensed MIT. Recharts is independent of MyBI.
